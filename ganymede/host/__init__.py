"""The host agent (docs/02-architecture-v2.md 7).

Runs on the contributor's machine, *outside* the container. Owns everything
about whether and what to run; the worker owns only doing the work (Finding E).
"""
